FAOSTATAPIClient.prototype.{{rel}} = function({{parameters}}, success, error) {
    if (this.isValidRequest()) {
        $.ajax({
            url: {{{url}}},
            data: {{{data}}},
            type: {{{method}}},
            success: success,
            error: error
        });
    } else {
        throw 400;
    }
};