/*global define*/
define(function ( ) {

    'use strict';

    return {

        ON_FILTER_CHANGE: 'fs.browse_by_domain.filter_change.event',
        ON_FILTER_INVALID_SELECTION: 'fs.browse_by_domain.invalid_selection.event'

    };
});

